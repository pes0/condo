{%- if message.meta.data.guestName -%}
{{ message.meta.data.guestName }} kommt an um {{ message.meta.data.openAt }}
{%- else -%}
    {%- if message.meta.data.vehicleBrand -%}
    {{ message.meta.data.vehicleBrand }}, {{ message.meta.data.stateVehicleNumber }} kommt an um {{ message.meta.data.openAt }}
    {%- else -%}
    {{ message.meta.data.stateVehicleNumber }} kommt an um {{ message.meta.data.openAt }}
    {% endif -%}
{% endif -%}
