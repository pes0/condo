System: {{ message.meta.os }}
Plattform: {% if message.meta.platform %}{{ message.meta.platform | safe }}{% else %}N/A{% endif %}
App-Name: {% if message.meta.app %}{{ message.meta.app | safe }}{% else %}N/A{% endif %}
App-Version: {{ message.meta.appVersion }}
User-Agent: {% if message.meta.userAgent %}{{ message.meta.userAgent | safe }}{% else %}N/A{% endif %}
E-Mail: {% if message.emailFrom %}{{ message.emailFrom | safe }}{% else %}N/A{% endif %}
Bewohner-Info:
{%- if message.meta.residentsExtraInfo.length == 0 -%}
  keine
{%- else -%}
 {% for residentInfo in message.meta.residentsExtraInfo %}
    Organisation: {% if residentInfo.organization %}{{ residentInfo.organization.name }}. UID: {{ residentInfo.organization.tin }}{% else %}N/A{% endif %}.
    Adresse: {% if residentInfo.address %}{{ residentInfo.address | safe }}{% else %}N/A{% endif %}.
    Einheit: {% if residentInfo.unitName %}{{ residentInfo.unitName | safe }}{% else %}N/A{% endif %}.
    Kontonummern:
        {%- if residentInfo.serviceConsumers.length == 0 -%}
          keine
        {%- else -%}
          {% for serviceConsumer in residentInfo.serviceConsumers %}
            Nummer: {% if serviceConsumer.accountNumber %}{{ serviceConsumer.accountNumber | safe }}{% else %}N/A{% endif %}.
            Organisation: {% if serviceConsumer.organizationName %}{{ serviceConsumer.organizationName | safe }}{% else %}N/A{% endif %}.

          {% endfor %}
        {% endif %}

 {% endfor %}
{% endif %}
Nachrichtentext: {{ message.meta.text | safe }}
